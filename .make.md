# Make コマンド一覧

## 開発コマンド

### `make dev`
全てのサービスを起動（DB + Backend + Frontend）

```bash
make dev
```

自動的に以下を実行：
1. データベース起動
2. 3秒待機（DB起動待ち）
3. バックエンドとフロントエンドを並列起動

### `make dev-front`
フロントエンドのみ起動

```bash
make dev-front
```

- Port: 3000

### `make dev-back`
バックエンドのみ起動（DBも自動起動）

```bash
make dev-back
```

自動的に以下を実行：
1. データベース起動
2. 3秒待機
3. バックエンド起動

- Port: 3001

### `make dev-db`
データベースのみ起動

```bash
make dev-db
```

- Port: 5432

### `make stop`
全てのサービスを停止

```bash
make stop
```

## データベース管理コマンド

### `make db`
データベースを起動

```bash
make db
```

### `make db-reset`
データベースをリセット（ボリューム削除して再作成）

```bash
make db-reset
```

自動的に以下を実行：
1. 既存のコンテナ停止
2. ボリューム削除
3. データベース起動
4. スキーマ適用
5. シードデータ投入

### `make db-studio`
Drizzle Studioを起動

```bash
make db-studio
```

ブラウザで `https://local.drizzle.studio` にアクセス

### `make db-down`
データベースを停止

```bash
make db-down
```

## Docker Composeコマンド

### `make up`
docker-compose up -d

```bash
make up
```

### `make down`
docker-compose down

```bash
make down
```

## ヘルプ

### `make help`
利用可能なコマンド一覧を表示

```bash
make help
```
